*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
.kotlin/
local.properties

# Signing keystores — never commit
*.jks
*.keystore

# SDD scratch (ledger/reports) — never commit
.superpowers/

# Credential env files — never commit. The BrowserStack runner reads
# ~/.browserstack.env from OUTSIDE the repo; this stops a stray copy landing here.
.env
*.env
