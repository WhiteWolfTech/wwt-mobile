*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing keystores — never commit
*.jks
*.keystore

# SDD scratch (ledger/reports) — never commit
.superpowers/
